<Window x:Class="QuickTechSystems.WPF.Views.Dialogs.ProductFilterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Advanced Product Filter" 
        Height="600" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F9FA">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="CategoryGroupStyle" TargetType="GroupBox">
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="CategoryComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Select categories to filter products. Leave empty to show all options for that category."
                   FontSize="14"
                   Foreground="#6C757D"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <GroupBox Header="Main Category" Style="{StaticResource CategoryGroupStyle}">
                    <ComboBox x:Name="CategoryComboBox"
                              Style="{StaticResource CategoryComboBoxStyle}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="CategoryId"/>
                </GroupBox>

                <GroupBox Header="Plants/Hardscape" Style="{StaticResource CategoryGroupStyle}">
                    <ComboBox x:Name="PlantsHardscapeComboBox"
                              Style="{StaticResource CategoryComboBoxStyle}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="CategoryId"/>
                </GroupBox>

                <GroupBox Header="Local/Imported" Style="{StaticResource CategoryGroupStyle}">
                    <ComboBox x:Name="LocalImportedComboBox"
                              Style="{StaticResource CategoryComboBoxStyle}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="CategoryId"/>
                </GroupBox>

                <GroupBox Header="Indoor/Outdoor" Style="{StaticResource CategoryGroupStyle}">
                    <ComboBox x:Name="IndoorOutdoorComboBox"
                              Style="{StaticResource CategoryComboBoxStyle}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="CategoryId"/>
                </GroupBox>

                <GroupBox Header="Plant Family" Style="{StaticResource CategoryGroupStyle}">
                    <ComboBox x:Name="PlantFamilyComboBox"
                              Style="{StaticResource CategoryComboBoxStyle}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="CategoryId"/>
                </GroupBox>

                <GroupBox Header="Detail" Style="{StaticResource CategoryGroupStyle}">
                    <ComboBox x:Name="DetailComboBox"
                              Style="{StaticResource CategoryComboBoxStyle}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="CategoryId"/>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <TextBlock Grid.Row="2" 
                   x:Name="FilterSummaryText"
                   FontSize="12"
                   Foreground="#28A745"
                   FontWeight="SemiBold"
                   Margin="0,10"
                   Visibility="{Binding HasFilterApplied, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Clear All" 
                    Style="{StaticResource DangerButtonStyle}"
                    Click="ClearAll_Click"/>
            <Button Content="Cancel" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="Cancel_Click"/>
            <Button Content="Apply Filter" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</Window>