<Window x:Class="QuickTechSystems.WPF.Views.ProductDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:QuickTechSystems.WPF.Views"
        mc:Ignorable="d"
        Title="Product Details"
        Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="White"
        BorderThickness="1"
        BorderBrush="#DDDDDD">

    <Window.Resources>
        <CollectionViewSource x:Key="PlantsHardscapeCategoriesViewSource" 
                           Source="{Binding PlantsHardscapeCategories}"/>
        <CollectionViewSource x:Key="LocalImportedCategoriesViewSource" 
                           Source="{Binding LocalImportedCategories}"/>
        <CollectionViewSource x:Key="IndoorOutdoorCategoriesViewSource" 
                           Source="{Binding IndoorOutdoorCategories}"/>
        <CollectionViewSource x:Key="PlantFamilyCategoriesViewSource" 
                           Source="{Binding PlantFamilyCategories}"/>
        <CollectionViewSource x:Key="DetailCategoriesViewSource" 
                           Source="{Binding DetailCategories}"/>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="TextInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                         Focusable="False" 
                                         HorizontalScrollBarVisibility="Hidden" 
                                         VerticalScrollBarVisibility="Hidden"
                                         Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="#007BFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderThickness="0"
                               CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0069D9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0062CC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderThickness="0"
                               CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A6268"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#545B62"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderThickness="0"
                               CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3D8B40"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2E7D32"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderThickness="0"
                               CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C82333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#BD2130"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#007BFF"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Height="50" VerticalAlignment="Top" Background="White" BorderThickness="0,0,0,1" BorderBrush="#DDDDDD" MouseDown="Border_MouseDown">
            <Grid>
                <TextBlock Text="Product Details" 
                          FontSize="18" 
                          FontWeight="SemiBold" 
                          VerticalAlignment="Center" 
                          Margin="20,0,0,0"/>
                <Button Content="✕" 
                        Click="CloseButton_Click"
                        Width="30" 
                        Height="30"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="16"
                        Foreground="#666666"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,15,0"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <ScrollViewer Margin="0,50,0,60" VerticalScrollBarVisibility="Auto">
            <Grid Margin="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <Border Style="{StaticResource SectionStyle}">
                        <StackPanel>
                            <TextBlock Text="Basic Information" Style="{StaticResource SectionTitleStyle}"/>

                            <TextBlock Text="Product Name *" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding SelectedProduct.Name, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextInputStyle}" 
                                    Margin="0,0,0,20"/>

                            <TextBlock Text="Purchase Price *" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding SelectedProduct.PurchasePrice, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextInputStyle}" 
                                    Tag="decimal"
                                    PreviewTextInput="DecimalTextBox_PreviewTextInput"
                                    DataObject.Pasting="NumericTextBox_Pasting"
                                    GotFocus="PriceTextBox_GotFocus"
                                    LostFocus="PriceTextBox_LostFocus"
                                    Margin="0,0,0,20"/>

                            <TextBlock Text="Sale Price *" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding SelectedProduct.SalePrice, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextInputStyle}" 
                                    Tag="decimal"
                                    PreviewTextInput="DecimalTextBox_PreviewTextInput"
                                    DataObject.Pasting="NumericTextBox_Pasting"
                                    GotFocus="PriceTextBox_GotFocus"
                                    LostFocus="PriceTextBox_LostFocus"
                                    Margin="0,0,0,20"/>

                            <TextBlock Text="Current Stock" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding SelectedProduct.CurrentStock, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextInputStyle}" 
                                    Tag="integer"
                                    PreviewTextInput="IntegerTextBox_PreviewTextInput"
                                    DataObject.Pasting="NumericTextBox_Pasting"
                                    Margin="0,0,0,20"/>

                            <CheckBox Content="Is Active" 
                                    IsChecked="{Binding SelectedProduct.IsActive}" 
                                    Margin="0,0,0,0"
                                    VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionStyle}">
                        <StackPanel>
                            <TextBlock Text="Barcode Management" Style="{StaticResource SectionTitleStyle}"/>

                            <TextBlock Text="Barcode" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding SelectedProduct.Barcode, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextInputStyle}" 
                                    Margin="0,0,0,15"/>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0"
                                        Content="Generate from Input"
                                        Command="{Binding GenerateBarcodeCommand}"
                                        Style="{StaticResource ActionButton}"/>
                                <Button Grid.Column="2"
                                        Content="Auto-Generate"
                                        Command="{Binding GenerateAutomaticBarcodeCommand}"
                                        Style="{StaticResource SecondaryButton}"/>
                            </Grid>

                            <TextBlock Text="Barcode Preview" Style="{StaticResource FieldLabelStyle}"/>
                            <Border BorderBrush="#DDDDDD"
                                    BorderThickness="1"
                                    Padding="10"
                                    Background="#F8F9FA"
                                    Height="80">
                                <Image Source="{Binding BarcodeImage}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <Border Style="{StaticResource SectionStyle}">
                        <StackPanel>
                            <TextBlock Text="Category Classifications" Style="{StaticResource SectionTitleStyle}"/>

                            <TextBlock Text="Plants/Hardscape" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource PlantsHardscapeCategoriesViewSource}}"
                                     DisplayMemberPath="Name"
                                     SelectedValuePath="CategoryId"
                                     SelectedValue="{Binding SelectedProduct.PlantsHardscapeId, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ComboBoxStyle}"
                                     Margin="0,0,0,15"/>

                            <TextBlock Text="Local/Imported" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource LocalImportedCategoriesViewSource}}"
                                     DisplayMemberPath="Name"
                                     SelectedValuePath="CategoryId"
                                     SelectedValue="{Binding SelectedProduct.LocalImportedId, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ComboBoxStyle}"
                                     Margin="0,0,0,15"/>

                            <TextBlock Text="Indoor/Outdoor" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource IndoorOutdoorCategoriesViewSource}}"
                                     DisplayMemberPath="Name"
                                     SelectedValuePath="CategoryId"
                                     SelectedValue="{Binding SelectedProduct.IndoorOutdoorId, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ComboBoxStyle}"
                                     Margin="0,0,0,15"/>

                            <TextBlock Text="Plant Family" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource PlantFamilyCategoriesViewSource}}"
                                     DisplayMemberPath="Name"
                                     SelectedValuePath="CategoryId"
                                     SelectedValue="{Binding SelectedProduct.PlantFamilyId, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ComboBoxStyle}"
                                     Margin="0,0,0,15"/>

                            <TextBlock Text="Detail Category" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Source={StaticResource DetailCategoriesViewSource}}"
                                     DisplayMemberPath="Name"
                                     SelectedValuePath="CategoryId"
                                     SelectedValue="{Binding SelectedProduct.DetailId, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ComboBoxStyle}"
                                     Margin="0,0,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionStyle}">
                        <StackPanel>
                            <TextBlock Text="Product Image" Style="{StaticResource SectionTitleStyle}"/>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0"
                                        Content="Upload Image"
                                        Command="{Binding UploadImageCommand}"
                                        Style="{StaticResource ActionButton}"/>
                                <Button Grid.Column="2"
                                        Content="Clear Image"
                                        Command="{Binding ClearImageCommand}"
                                        Style="{StaticResource SecondaryButton}"/>
                            </Grid>

                            <Border BorderBrush="#DDDDDD"
                                    BorderThickness="1"
                                    Height="180"
                                    Background="#F8F9FA">
                                <Image Source="{Binding ProductImage}"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       Margin="5"/>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <Border Height="60" VerticalAlignment="Bottom" Background="White" BorderThickness="0,1,0,0" BorderBrush="#DDDDDD">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,30,0">
                    <Button Content="Delete"
                            Command="{Binding DeleteCommand}"
                            Click="DeleteButton_Click"
                            Style="{StaticResource DangerButton}"
                            Width="100"
                            Margin="0,0,15,0"
                            Visibility="{Binding IsNewProduct, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                    <Button Content="Save"
                            Command="{Binding SaveCommand}"
                            Click="SaveButton_Click"
                            Style="{StaticResource SuccessButton}"
                            Width="100"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>