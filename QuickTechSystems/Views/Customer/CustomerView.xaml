<UserControl x:Class="QuickTechSystems.WPF.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:domain="clr-namespace:QuickTechSystems.Domain.Entities;assembly=QuickTechSystems.Domain"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <ObjectDataProvider x:Key="PaymentStatusValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="sys:String"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <Grid Background="{StaticResource PrimaryBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Style="{StaticResource CardBorderStyle}"
                Margin="0,0,0,16">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="👥" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Text="{DynamicResource Customer_Management}" 
                              FontSize="24" 
                              FontWeight="SemiBold" 
                              Foreground="{StaticResource TextPrimaryBrush}"
                              VerticalAlignment="Center"/>
                </StackPanel>

                <TextBox Grid.Column="1"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        Width="300"
                        Height="36"
                        Margin="0,0,16,0"
                        Padding="12,0"
                        BorderBrush="{StaticResource BorderBrush}"
                        Background="{StaticResource SurfaceBrush}"
                        VerticalContentAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                               BorderBrush="{TemplateBinding BorderBrush}"
                                               BorderThickness="1"
                                               CornerRadius="4">
                                            <Grid>
                                                <TextBlock Text="{DynamicResource Customer_SearchPlaceholder}"
                                                         Foreground="{StaticResource TextTertiaryBrush}"
                                                         Margin="12,0"
                                                         VerticalAlignment="Center"
                                                         IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <ScrollViewer x:Name="PART_ContentHost"
                                                            Margin="12,0"
                                                            VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Column="2"
                       Command="{Binding AddCustomerCommand}"
                       Style="{StaticResource PrimaryButtonStyle}"
                       Height="36"
                       Padding="16,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontWeight="Bold" Margin="0,0,8,0"/>
                        <TextBlock Text="{DynamicResource Customer_AddCustomer}"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                   Style="{StaticResource CardBorderStyle}"
                   Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                              Text="{DynamicResource Customer_CustomerList}"
                              FontSize="18"
                              FontWeight="SemiBold"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Margin="20,20,20,16"/>

                    <DataGrid Grid.Row="1"
                             ItemsSource="{Binding Customers}"
                             SelectedItem="{Binding SelectedCustomer}"
                             Style="{StaticResource ModernDataGridStyle}"
                             Margin="20,0"
                             AutoGenerateColumns="False"
                             SelectionMode="Single"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource Customer_Name}" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="{DynamicResource Customer_Phone}" Binding="{Binding Phone}" Width="100"/>
                            <DataGridTextColumn Header="{DynamicResource Customer_Balance}" Binding="{Binding Balance, StringFormat=C}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Balance}" Value="0">
                                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=Balance, Converter={StaticResource LessThanZeroConverter}}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource ErrorTextBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                        <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" 
                               Orientation="Horizontal" 
                               Margin="20"
                               HorizontalAlignment="Right">
                        <Button Command="{Binding EditCustomerCommand}"
                               Style="{StaticResource SecondaryButtonStyle}"
                               Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✏️" Margin="0,0,6,0"/>
                                <TextBlock Text="{DynamicResource Customer_Edit}"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding DeleteCustomerCommand}"
                               Style="{StaticResource SecondaryButtonStyle}"
                               Background="#FFF5F8"
                               BorderBrush="#F3D4DA"
                               Foreground="#9A4355">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🗑️" Margin="0,0,6,0"/>
                                <TextBlock Text="{DynamicResource Customer_Delete}"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" 
                   Style="{StaticResource CardBorderStyle}"
                   Margin="8,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20">

                        <Border Background="{StaticResource ErrorLightBrush}"
                               BorderBrush="{StaticResource ErrorBorderBrush}"
                               BorderThickness="1"
                               CornerRadius="6"
                               Padding="16"
                               Margin="0,0,0,20"
                               Visibility="{Binding IsAddingCustomer, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                                          Text="{DynamicResource Customer_AddNewCustomer}"
                                          FontSize="18"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          Margin="0,0,0,16"/>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
                                    <TextBlock Text="{DynamicResource Customer_NameRequired}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Name, UpdateSourceTrigger=PropertyChanged}"
                                            Height="36" Padding="8"/>

                                    <TextBlock Text="{DynamicResource Customer_Phone}" FontWeight="SemiBold" Margin="0,12,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Phone, UpdateSourceTrigger=PropertyChanged}"
                                            Height="36" Padding="8"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,0">
                                    <TextBlock Text="{DynamicResource Customer_Email}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Email, UpdateSourceTrigger=PropertyChanged}"
                                            Height="36" Padding="8"/>

                                    <TextBlock Text="{DynamicResource Customer_Address}" FontWeight="SemiBold" Margin="0,12,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Address, UpdateSourceTrigger=PropertyChanged}"
                                            Height="60" Padding="8" TextWrapping="Wrap" AcceptsReturn="True"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" 
                                           Orientation="Horizontal" 
                                           HorizontalAlignment="Right" 
                                           Margin="0,16,0,0">
                                    <Button Command="{Binding CancelEditCommand}"
                                           Style="{StaticResource SecondaryButtonStyle}"
                                           Margin="0,0,8,0"
                                           Content="{DynamicResource Customer_Cancel}"/>
                                    <Button Command="{Binding SaveCustomerCommand}"
                                           Style="{StaticResource PrimaryButtonStyle}"
                                           Content="{DynamicResource Customer_SaveCustomer}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Background="{StaticResource HoverBrush}"
                               BorderBrush="{StaticResource AccentBrush}"
                               BorderThickness="1"
                               CornerRadius="6"
                               Padding="16"
                               Margin="0,0,0,20"
                               Visibility="{Binding IsEditingCustomer, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                                          Text="{DynamicResource Customer_EditCustomer}"
                                          FontSize="18"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource TextPrimaryBrush}"
                                          Margin="0,0,0,16"/>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
                                    <TextBlock Text="{DynamicResource Customer_NameRequired}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Name, UpdateSourceTrigger=PropertyChanged}"
                                            Height="36" Padding="8"/>

                                    <TextBlock Text="{DynamicResource Customer_Phone}" FontWeight="SemiBold" Margin="0,12,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Phone, UpdateSourceTrigger=PropertyChanged}"
                                            Height="36" Padding="8"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,0">
                                    <TextBlock Text="{DynamicResource Customer_Email}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Email, UpdateSourceTrigger=PropertyChanged}"
                                            Height="36" Padding="8"/>

                                    <TextBlock Text="{DynamicResource Customer_Address}" FontWeight="SemiBold" Margin="0,12,0,4"/>
                                    <TextBox Text="{Binding EditingCustomer.Address, UpdateSourceTrigger=PropertyChanged}"
                                            Height="60" Padding="8" TextWrapping="Wrap" AcceptsReturn="True"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" 
                                           Orientation="Horizontal" 
                                           HorizontalAlignment="Right" 
                                           Margin="0,16,0,0">
                                    <Button Command="{Binding CancelEditCommand}"
                                           Style="{StaticResource SecondaryButtonStyle}"
                                           Margin="0,0,8,0"
                                           Content="{DynamicResource Customer_Cancel}"/>
                                    <Button Command="{Binding SaveCustomerCommand}"
                                           Style="{StaticResource PrimaryButtonStyle}"
                                           Content="{DynamicResource Customer_UpdateCustomer}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <StackPanel Visibility="{Binding SelectedCustomer, Converter={StaticResource NullToVisibilityConverter}}">
                            <TextBlock Text="{DynamicResource Customer_CustomerDetails}"
                                      FontSize="20"
                                      FontWeight="SemiBold"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      Margin="0,0,0,16"/>

                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" 
                                       Background="{StaticResource SurfaceBrush}"
                                       BorderBrush="{StaticResource BorderBrush}"
                                       BorderThickness="1"
                                       CornerRadius="6"
                                       Padding="16"
                                       Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="📱" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="{DynamicResource Customer_Phone}" 
                                                  FontWeight="SemiBold" 
                                                  HorizontalAlignment="Center"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding SelectedCustomer.Phone}" 
                                                  HorizontalAlignment="Center"
                                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="1" 
                                       Background="{StaticResource SurfaceBrush}"
                                       BorderBrush="{StaticResource BorderBrush}"
                                       BorderThickness="1"
                                       CornerRadius="6"
                                       Padding="16"
                                       Margin="4,0">
                                    <StackPanel>
                                        <TextBlock Text="💰" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="{DynamicResource Customer_Balance}" 
                                                  FontWeight="SemiBold" 
                                                  HorizontalAlignment="Center"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding SelectedCustomer.Balance, StringFormat=C}" 
                                                  HorizontalAlignment="Center"
                                                  FontSize="16"
                                                  FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SelectedCustomer.Balance}" Value="0">
                                                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Path=SelectedCustomer.Balance, Converter={StaticResource LessThanZeroConverter}}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource ErrorTextBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="2" 
                                       Background="{StaticResource SurfaceBrush}"
                                       BorderBrush="{StaticResource BorderBrush}"
                                       BorderThickness="1"
                                       CornerRadius="6"
                                       Padding="16"
                                       Margin="8,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="🛒" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="{DynamicResource Customer_Payments}" 
                                                  FontWeight="SemiBold" 
                                                  HorizontalAlignment="Center"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding SelectedCustomer.TransactionCount}" 
                                                  HorizontalAlignment="Center"
                                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <UniformGrid Columns="4" Margin="0,0,0,20">
                                <Button Command="{Binding EditBalanceCommand}"
                                       Style="{StaticResource PrimaryButtonStyle}"
                                       Margin="0,0,4,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="💳" Margin="0,0,6,0"/>
                                        <TextBlock Text="{DynamicResource Customer_EditBalance}"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding SettleDebtCommand}"
                                       Style="{StaticResource PrimaryButtonStyle}"
                                       Margin="2,0"
                                       Background="{StaticResource SuccessBrush}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="💸" Margin="0,0,6,0"/>
                                        <TextBlock Text="{DynamicResource Customer_SettleDebt}"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding ShowHistoryCommand}"
                                       Style="{StaticResource SecondaryButtonStyle}"
                                       Margin="2,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📊" Margin="0,0,6,0"/>
                                        <TextBlock Text="{DynamicResource Customer_History}"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding RefreshCommand}"
                                       Style="{StaticResource SecondaryButtonStyle}"
                                       Margin="4,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔄" Margin="0,0,6,0"/>
                                        <TextBlock Text="{DynamicResource Customer_Refresh}"/>
                                    </StackPanel>
                                </Button>
                            </UniformGrid>

                            <Border Background="{StaticResource HoverBrush}"
                                   BorderBrush="{StaticResource AccentBrush}"
                                   BorderThickness="1"
                                   CornerRadius="6"
                                   Padding="16"
                                   Margin="0,0,0,20"
                                   Visibility="{Binding IsEditingBalance, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Customer_EditBalance}"
                                              FontSize="16"
                                              FontWeight="SemiBold"
                                              Margin="0,0,0,12"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                            <TextBlock Text="{DynamicResource Customer_NewBalance}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding NewBalance, UpdateSourceTrigger=PropertyChanged}"
                                                    Height="36" Padding="8"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                            <TextBlock Text="{DynamicResource Customer_ReasonRequired}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding AdjustmentReason, UpdateSourceTrigger=PropertyChanged}"
                                                    Height="36" Padding="8"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" 
                                               HorizontalAlignment="Right" 
                                               Margin="0,12,0,0">
                                        <Button Command="{Binding CancelEditCommand}"
                                               Style="{StaticResource SecondaryButtonStyle}"
                                               Margin="0,0,8,0"
                                               Content="{DynamicResource Customer_Cancel}"/>
                                        <Button Command="{Binding SaveBalanceCommand}"
                                               Style="{StaticResource PrimaryButtonStyle}"
                                               Content="{DynamicResource Customer_SaveBalance}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Background="#F0F9FF"
                                   BorderBrush="{StaticResource SuccessBrush}"
                                   BorderThickness="1"
                                   CornerRadius="6"
                                   Padding="16"
                                   Margin="0,0,0,20"
                                   Visibility="{Binding IsSettlingDebt, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Customer_SettleDebt}"
                                              FontSize="16"
                                              FontWeight="SemiBold"
                                              Margin="0,0,0,12"/>

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,0">
                                            <TextBlock Text="{DynamicResource Customer_PaymentAmount}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged}"
                                                    Height="36" Padding="8"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,0">
                                            <TextBlock Text="{DynamicResource Customer_PaymentMethod}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <ComboBox Text="{Binding PaymentMethod, UpdateSourceTrigger=PropertyChanged}"
                                                     Height="36" Padding="8" IsEditable="True">
                                                <sys:String>Cash</sys:String>
                                                <sys:String>Credit Card</sys:String>
                                                <sys:String>Debit Card</sys:String>
                                                <sys:String>Bank Transfer</sys:String>
                                                <sys:String>Check</sys:String>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,12,0,0">
                                            <TextBlock Text="{DynamicResource Customer_PaymentNotes}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding PaymentNotes, UpdateSourceTrigger=PropertyChanged}"
                                                    Height="36" Padding="8"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" 
                                               HorizontalAlignment="Right" 
                                               Margin="0,12,0,0">
                                        <Button Command="{Binding CancelEditCommand}"
                                               Style="{StaticResource SecondaryButtonStyle}"
                                               Margin="0,0,8,0"
                                               Content="{DynamicResource Customer_Cancel}"/>
                                        <Button Command="{Binding ProcessPaymentCommand}"
                                               Style="{StaticResource PrimaryButtonStyle}"
                                               Background="{StaticResource SuccessBrush}"
                                               Content="{DynamicResource Customer_ProcessPayment}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Background="#F0F9FF"
                                   BorderBrush="{StaticResource AccentBrush}"
                                   BorderThickness="1"
                                   CornerRadius="6"
                                   Padding="16"
                                   Margin="0,0,0,20"
                                   Visibility="{Binding IsEditingPayment, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="{DynamicResource Customer_EditPayment}"
                                              FontSize="16"
                                              FontWeight="SemiBold"
                                              Margin="0,0,0,12"/>

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,0">
                                            <TextBlock Text="{DynamicResource Customer_AmountRequired}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding EditingPayment.Amount, UpdateSourceTrigger=PropertyChanged}"
                                                    Height="36" Padding="8"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,0">
                                            <TextBlock Text="{DynamicResource Customer_PaymentMethodRequired}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <ComboBox Text="{Binding EditingPayment.PaymentMethod, UpdateSourceTrigger=PropertyChanged}"
                                                     Height="36" Padding="8" IsEditable="True">
                                                <sys:String>Cash</sys:String>
                                                <sys:String>Credit Card</sys:String>
                                                <sys:String>Debit Card</sys:String>
                                                <sys:String>Bank Transfer</sys:String>
                                                <sys:String>Check</sys:String>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,12,8,0">
                                            <TextBlock Text="{DynamicResource Customer_PaymentDate}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <DatePicker SelectedDate="{Binding EditingPayment.PaymentDate, UpdateSourceTrigger=PropertyChanged}"
                                                       Height="36"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,12,0,0">
                                            <TextBlock Text="{DynamicResource Customer_Status}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <ComboBox Text="{Binding EditingPayment.Status, UpdateSourceTrigger=PropertyChanged}"
                                                     Height="36" Padding="8" IsEditable="True">
                                                <sys:String>Completed</sys:String>
                                                <sys:String>Pending</sys:String>
                                                <sys:String>Cancelled</sys:String>
                                            </ComboBox>
                                        </StackPanel>

                                        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,12,0,0">
                                            <TextBlock Text="{DynamicResource Customer_Notes}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBox Text="{Binding EditingPayment.Notes, UpdateSourceTrigger=PropertyChanged}"
                                                    Height="60" Padding="8" TextWrapping="Wrap" AcceptsReturn="True"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" 
                                               HorizontalAlignment="Right" 
                                               Margin="0,16,0,0">
                                        <Button Command="{Binding CancelEditCommand}"
                                               Style="{StaticResource SecondaryButtonStyle}"
                                               Margin="0,0,8,0"
                                               Content="{DynamicResource Customer_Cancel}"/>
                                        <Button Command="{Binding SavePaymentCommand}"
                                               Style="{StaticResource PrimaryButtonStyle}"
                                               Content="{DynamicResource Customer_SavePayment}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardBorderStyle}"
                                   Margin="0,0,0,20"
                                   Visibility="{Binding IsShowingHistory, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel Margin="16">
                                    <TextBlock Text="{DynamicResource Customer_PaymentHistory}"
                                              FontSize="16"
                                              FontWeight="SemiBold"
                                              Margin="0,0,0,12"/>

                                    <DataGrid ItemsSource="{Binding CustomerPayments}"
                                             SelectedItem="{Binding SelectedPayment}"
                                             Style="{StaticResource ModernDataGridStyle}"
                                             AutoGenerateColumns="False"
                                             MaxHeight="300">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="{DynamicResource Customer_Date}" 
                                                               Binding="{Binding PaymentDate, StringFormat=MM/dd/yyyy}" 
                                                               Width="100"/>
                                            <DataGridTextColumn Header="{DynamicResource Customer_Amount}" 
                                                               Binding="{Binding Amount, StringFormat=C}" 
                                                               Width="100"/>
                                            <DataGridTextColumn Header="{DynamicResource Customer_PaymentMethod}" 
                                                               Binding="{Binding PaymentMethod}" 
                                                               Width="120"/>
                                            <DataGridTextColumn Header="{DynamicResource Customer_Status}" 
                                                               Binding="{Binding Status}" 
                                                               Width="80"/>
                                            <DataGridTextColumn Header="{DynamicResource Customer_Notes}" 
                                                               Binding="{Binding Notes}" 
                                                               Width="*"/>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <Grid Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" 
                                                   Orientation="Horizontal" 
                                                   HorizontalAlignment="Left">
                                            <Button Command="{Binding EditPaymentCommand}"
                                                   Style="{StaticResource SecondaryButtonStyle}"
                                                   Margin="0,0,8,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="✏️" Margin="0,0,6,0"/>
                                                    <TextBlock Text="{DynamicResource Customer_EditPayment}"/>
                                                </StackPanel>
                                            </Button>

                                            <Button Command="{Binding DeletePaymentCommand}"
                                                   Style="{StaticResource SecondaryButtonStyle}"
                                                   Background="#FFF5F8"
                                                   BorderBrush="#F3D4DA"
                                                   Foreground="#9A4355">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="🗑️" Margin="0,0,6,0"/>
                                                    <TextBlock Text="{DynamicResource Customer_DeletePayment}"/>
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>

                                        <Button Grid.Column="1"
                                               Command="{Binding HideHistoryCommand}"
                                               Style="{StaticResource SecondaryButtonStyle}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="❌" Margin="0,0,6,0"/>
                                                <TextBlock Text="{DynamicResource Customer_CloseHistory}"/>
                                            </StackPanel>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>