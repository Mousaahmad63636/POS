<!-- Path: QuickTechSystems/Views/ProductView.xaml -->
<UserControl x:Class="QuickTechSystems.WPF.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:QuickTechSystems.WPF.Views"
             xmlns:converters="clr-namespace:QuickTechSystems.WPF.Converters"
             mc:Ignorable="d"
             x:Name="ProductViewRoot"
             FlowDirection="{Binding FlowDirection}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:ByteArrayToImageSourceConverter x:Key="ByteArrayToImageConverter"/>
        <converters:ProductTotalCostConverter x:Key="ProductTotalCostConverter"/>
        <converters:ProductTotalValueConverter x:Key="ProductTotalValueConverter"/>
        <converters:ProductTotalProfitConverter x:Key="ProductTotalProfitConverter"/>
        <converters:ProfitColorConverter x:Key="ProfitColorConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToTextConverter x:Key="BoolToTextConverter"/>

        <!-- Mac-inspired Styles -->
        <Style x:Key="MacButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#0069C0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.2" Color="#000000"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                                <Setter Property="Foreground" Value="#757575"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MacSuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource MacButtonStyle}">
            <Setter Property="Background" Value="#00897B"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00695C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MacCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Direction="270" Opacity="0.15" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MacDataGrid" TargetType="DataGrid" BasedOn="{StaticResource ResponsiveDataGrid}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="VerticalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="RowHeight" Value="48"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="True"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="MacDataGridColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="12,12"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid>
                            <Border x:Name="BackgroundBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ContentPresenter Grid.Row="0"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEF2F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MacDataGridCell" TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="Foreground" Value="#000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsMouseOver}" Value="True">
                    <Setter Property="Background" Value="#F5F7FA"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MacTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="TabStripPlacement" Value="Top"/>
        </Style>

        <Style x:Key="MacTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border x:Name="Border" 
                                    Background="Transparent" 
                                    BorderThickness="0" 
                                    CornerRadius="4,4,0,0" 
                                    Margin="{TemplateBinding Margin}">
                                <ContentPresenter x:Name="ContentSite"
                                                 VerticalAlignment="Center"
                                                 HorizontalAlignment="Center"
                                                 ContentSource="Header"
                                                 Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <Border x:Name="SelectedIndicator" 
                                    Height="3" 
                                    Background="#0069C0" 
                                    VerticalAlignment="Bottom"
                                    CornerRadius="1.5,1.5,0,0"
                                    Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectedIndicator" Property="Opacity" Value="1"/>
                                <Setter Property="Foreground" Value="#0069C0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F5F7FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MacSearchBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                             Margin="{TemplateBinding Padding}"
                                             VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEF2F7"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="#0069C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for right-aligned numeric cells -->
        <Style x:Key="MacRightAlignedCell" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Style for text-based action buttons in DataGrid -->
        <Style x:Key="MacTextButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#0069C0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Status indicator style -->
        <Style x:Key="MacStatusIndicator" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F7FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <TabControl Style="{StaticResource MacTabControl}" SelectionChanged="TabControl_SelectionChanged">
                <TabItem Header="{DynamicResource Product_TabProducts}" Style="{StaticResource MacTabItem}">
                    <Border Padding="24" Background="Transparent">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Page Title -->
                            <TextBlock Grid.Row="0" 
                                       Text="{DynamicResource Product_Title}"
                                       FontSize="26"
                                       FontWeight="SemiBold"
                                       Foreground="#212121"
                                       Margin="0,0,0,24"/>

                            <!-- Control Sections Row -->
                            <Grid Grid.Row="1" Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Add Buttons -->
                                <Border Style="{StaticResource MacCardStyle}" Grid.Column="0" Margin="0,0,16,0">
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="{DynamicResource Product_AddNew}" 
                                                Command="{Binding AddCommand}"
                                                Style="{StaticResource MacSuccessButtonStyle}" 
                                                Margin="0,0,8,0"/>
                                        <Button Content="{DynamicResource Product_BulkAdd}"
                                                Command="{Binding BulkAddCommand}"
                                                Style="{StaticResource MacButtonStyle}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Stock Update Section -->
                                <Border Style="{StaticResource MacCardStyle}" Grid.Column="1" Margin="0,0,16,0">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{DynamicResource Product_QuickStockUpdate}" 
                                                  FontWeight="SemiBold"
                                                  FontSize="14"
                                                  Margin="0,0,0,8"/>
                                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                                            <TextBox Width="80" 
                                                    Style="{StaticResource MacSearchBox}"
                                                    Text="{Binding StockIncrement, UpdateSourceTrigger=PropertyChanged}"
                                                    Margin="0,0,8,0"/>
                                            <Button Content="{DynamicResource Product_AddToStock}"
                                                    Command="{Binding UpdateStockCommand}"
                                                    Style="{StaticResource MacButtonStyle}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Barcode Printing Section -->
                                <Border Style="{StaticResource MacCardStyle}" Grid.Column="3">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="{DynamicResource Product_BarcodePrinting}" 
                                                  FontWeight="SemiBold"
                                                  FontSize="14"
                                                  Margin="0,0,0,8"/>

                                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource Product_NumberOfLabels}" 
                                                     FontSize="14"
                                                     VerticalAlignment="Center"
                                                     Margin="0,0,8,0"/>

                                            <TextBox Width="50"
                                                    Style="{StaticResource MacSearchBox}"
                                                    Text="{Binding LabelsPerProduct, UpdateSourceTrigger=PropertyChanged}"
                                                    VerticalAlignment="Center"
                                                    Height="36"
                                                    Margin="0,0,8,0"/>

                                            <Button Content="{DynamicResource Product_PrintBarcode}"
                                                    Command="{Binding PrintBarcodeCommand}"
                                                    Style="{StaticResource MacButtonStyle}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>

                            <!-- Search Box -->
                            <Border Grid.Row="2" Style="{StaticResource MacCardStyle}" Margin="0,0,0,24">
                                <Grid>
                                    <TextBox x:Name="searchBox" 
                                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource MacSearchBox}" 
                                             Margin="0"/>
                                    <TextBlock IsHitTestVisible="False" 
                                               Text="{DynamicResource Product_SearchPlaceholder}" 
                                               VerticalAlignment="Center" 
                                               Margin="24,0,0,0" 
                                               Foreground="#9E9E9E"
                                               FontSize="14">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=searchBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>

                            <!-- Product DataGrid -->
                            <Border Grid.Row="3" Style="{StaticResource MacCardStyle}">
                                <DataGrid ItemsSource="{Binding Products}"
                                          SelectedItem="{Binding SelectedProduct}"
                                          Style="{StaticResource MacDataGrid}"
                                          ColumnHeaderStyle="{StaticResource MacDataGridColumnHeader}"
                                          CellStyle="{StaticResource MacDataGridCell}"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True"
                                          IsEnabled="{Binding IsNotSaving}"
                                          BorderThickness="0"
                                          MouseDoubleClick="DataGrid_MouseDoubleClick">
                                    <DataGrid.Columns>
                                        <!-- Image column with fixed width -->
                                        <DataGridTemplateColumn Header="{DynamicResource Product_Image}" Width="70">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Border Width="48" Height="48" Background="#F5F7FA" CornerRadius="6" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,3">
                                                        <Image Source="{Binding Image, Converter={StaticResource ByteArrayToImageConverter}, ConverterParameter=40}" 
                                                               Height="40" 
                                                               Width="40" 
                                                               Stretch="Uniform"
                                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    </Border>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <!-- Main product information columns -->
                                        <DataGridTextColumn Header="{DynamicResource Product_Barcode}" 
                                                            Binding="{Binding Barcode}"
                                                            Width="120">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="FontFamily" Value="Consolas, Courier New, Monospace"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <DataGridTextColumn Header="{DynamicResource Product_Name}" 
                                                            Binding="{Binding Name}"
                                                            Width="*"
                                                            MinWidth="200">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                                    <Setter Property="FontWeight" Value="Medium"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <DataGridTextColumn Header="{DynamicResource Product_Category}" 
                                                            Binding="{Binding CategoryName}"
                                                            Width="130">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <!-- Inventory columns -->
                                        <DataGridTextColumn Header="{DynamicResource Product_CurrentStock}" 
                                                            Binding="{Binding CurrentStock}"
                                                            Width="90">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource MacRightAlignedCell}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CurrentStock}" Value="0">
                                                            <Setter Property="Foreground" Value="#E53935"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <!-- Price columns with right-alignment -->
                                        <DataGridTextColumn Header="{DynamicResource Product_PurchasePrice}" 
                                                            Binding="{Binding PurchasePrice, StringFormat=N}"
                                                            Width="120">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource MacRightAlignedCell}"/>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <DataGridTextColumn Header="{DynamicResource Product_SalePrice}" 
                                                            Binding="{Binding SalePrice, StringFormat=N}"
                                                            Width="120">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource MacRightAlignedCell}">
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                    <Setter Property="Foreground" Value="#00796B"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <!-- Optional columns that can be placed at the end -->
                                        <DataGridTextColumn Header="{DynamicResource Product_Speed}" 
                                                            Binding="{Binding Speed}"
                                                            Width="80">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource MacRightAlignedCell}"/>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <DataGridTextColumn Header="{DynamicResource Product_Supplier}" 
                                                            Binding="{Binding SupplierName}"
                                                            Width="140">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <!-- Status column -->
                                        <DataGridTemplateColumn Header="{DynamicResource Product_IsActive}" Width="90">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource MacStatusIndicator}"
                                                            Background="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}">
                                                        <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Active|Inactive'}" 
                                                                   Foreground="White" 
                                                                   FontSize="12" 
                                                                   FontWeight="SemiBold"
                                                                   HorizontalAlignment="Center"/>
                                                    </Border>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <!-- Action buttons column -->
                                        <DataGridTemplateColumn Width="120">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="{DynamicResource Product_Edit}"
                                                                Click="EditButton_Click"
                                                                Style="{StaticResource MacTextButtonStyle}"
                                                                Margin="0,0,8,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>

                                    <DataGrid.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="{DynamicResource Product_EditMenuItem}" Click="EditMenuItem_Click"/>
                                            <Separator/>
                                            <MenuItem Header="{DynamicResource Product_DeleteMenuItem}" Command="{Binding DeleteCommand}"/>
                                        </ContextMenu>
                                    </DataGrid.ContextMenu>
                                </DataGrid>
                            </Border>

                            <!-- Loading Overlay -->
                            <Border Grid.RowSpan="4"
                                   Style="{StaticResource LoadingOverlay}" 
                                   Visibility="{Binding IsSaving, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Border Background="White"
                                        CornerRadius="12"
                                        Padding="24"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Effect="{StaticResource DropShadowEffect}">
                                    <StackPanel>
                                        <ProgressBar IsIndeterminate="True"
                                                     Width="240"
                                                     Height="4"
                                                     Margin="0,0,0,16"
                                                     Foreground="#0069C0"/>
                                        <TextBlock Text="{Binding StatusMessage}"
                                                  FontSize="16"
                                                  HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Border>

                            <!-- Product Popup -->
                            <Popup IsOpen="{Binding IsProductPopupOpen, Mode=TwoWay}"
                                   StaysOpen="True"
                                   AllowsTransparency="True"
                                   PlacementTarget="{Binding ElementName=ProductViewRoot}"
                                   Placement="Center"
                                   PopupAnimation="Fade"
                                   Focusable="True"
                                   Width="{Binding ActualWidth, ElementName=ProductViewRoot}"
                                   Height="{Binding ActualHeight, ElementName=ProductViewRoot}">
                                <!-- Fixed reference to ProductDetailsPopup -->
                                <ContentControl Name="ProductDetailsPopupContainer">
                                    <local:ProductDetailsPopup DataContext="{Binding}"
                                                          CloseRequested="ProductDetailsPopup_CloseRequested"
                                                          SaveCompleted="ProductDetailsPopup_SaveCompleted"/>
                                </ContentControl>
                            </Popup>
                        </Grid>
                    </Border>
                </TabItem>

                <TabItem Header="{DynamicResource Product_TabDamagedGoods}" Style="{StaticResource MacTabItem}">
                    <!-- Use a ContentPresenter which can be dynamically loaded later -->
                    <Border Padding="24" Background="Transparent">
                        <ContentPresenter x:Name="DamagedGoodsContent">
                            <ContentPresenter.Content>
                                <TextBlock Text="{DynamicResource Product_DamagedGoodsImplementation}" 
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="18"/>
                            </ContentPresenter.Content>
                        </ContentPresenter>
                    </Border>
                </TabItem>
            </TabControl>
        </ScrollViewer>
    </Grid>
</UserControl>